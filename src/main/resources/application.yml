server:
  port: 8088
  reactive:
    session:
      timeout: 60s

spring:
  application:
    name: forum
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/db_forum?useUnicode=true&useSSL=true&characterEncoding=utf8
    username: root
    password: 1234
    druid:
      initial-size: 1 # ??????
      min-idle: 1 # ?????
      max-active: 20 # ?????
      test-on-borrow: true # ???????????????

      stat-view-servlet:
        allow: true # ?????????html??, ???????JSON API
#    mvc:
#      pathmatch:
#        matching-strategy: ant_path_matcher

  # ??????
  jackson:
    time-zone: GMT+8

# Mybatis-plus ??
mybatis-plus:
  # ??Mybatis????
  mapper-locations: classpath:mapper/*.xml
  # ??Mybatis?????????????????
  type-aliases-package: wlong.work.forum.domain
  configuration:
    #???????????????????????????????????????
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  global-config:
    db-config:
      table-prefix: tb_
forum:
  path: D:\file\image\
